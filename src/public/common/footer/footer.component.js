'use strict';

import template from './footer.html';

const navbarComponent = () => {
    return {
        restrict: 'E',
        scope: {},
        template
    };
};

export default navbarComponent;
